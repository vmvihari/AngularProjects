import { SkeletonLoaderDirective } from './skeleton-loader.directive';

describe('SkeletonLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeletonLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
